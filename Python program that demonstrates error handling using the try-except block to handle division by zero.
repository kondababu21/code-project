def divide_numbers(a, b):
    try:
        result = a / b
        print("Result:", result)
    except ZeroDivisionError:
        print("Error: Cannot divide by zero.")

# Example usage
divide_numbers(10, 2)   # Valid division
divide_numbers(5, 0)    # Will trigger ZeroDivisionError
