class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __sub__(self, other):
        return Vector(self.x - other.x, self.y - other.y)

    def __str__(self):
        return f"({self.x}, {self.y})"

# Test
v1 = Vector(5, 4)
v2 = Vector(2, 1)
print("Subtraction Result:", v1 - v2)
